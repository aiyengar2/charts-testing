# (optional) Include specific values that need to be overridden in either the main chart or the additionalCharts
# It is expected that the valuesOverride.additionalCharts are in the same order as that of the package.yaml
valuesOverride:
  chart: {}
    # prometheus:
    #   enabled: true
  additionalCharts: []
  # - image:
  #     repository: rancher/kubectl
  #     tag: v1.20.3

# Paths to files or globs for unit tests in packages/<package>/tests
unitTests:
- path: "unit/*"

# Paths to files or globs for integration tests in packages/<package>/tests
integrationTests:
- path: "integration/*"

# Paths to files or globs for validation tests in packages/<package>/tests
validationTests: []